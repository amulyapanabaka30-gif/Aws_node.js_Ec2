# Node.js Application Deployment on AWS EC2

## Overview
This project demonstrates deploying a Node.js application locally and hosting it on AWS EC2 for public access.

## Features
- Runs locally on port 3000
- Uses environment variables (.env) for configuration
- Hosted on AWS EC2 instance
- Accessible via public IP
